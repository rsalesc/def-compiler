def int mod(int x, int MOD){
  int q = x/MOD;
  return x-q*MOD;
}

def void solve(int x){ // for x > 0
  int i = 1;
  while(i*i <= x){
    if(!mod(x, i)){
      print(i);
      if(i*i != x){
        print(x/i);
      }
    }

    i = i+1;
  }

  print(-1);
}

def void main(){
  solve(8);
  solve(81);
  solve(47);
  solve(42);
  solve(56);
}
